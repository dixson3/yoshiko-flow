---
type: Review
okf_spec: OKF-PLAN
plan: plan-031-james-dixson-62a375
pass: 1
verdict: APPROVE
date: '2026-07-22'
---
# Red-Team Review — Pass 1

**Plan:** plan-031-james-dixson-62a375
**Date:** 2026-07-22
**Final status:** resolved

## Verdict: APPROVE

## Strengths

- Recon is accurate and specific; every spot-checked claim held (skill count 18, group taxonomy beads 8 / utility 6 / markdown 4, naba script behaviors, README suppression note, Route53 zone).
- Reuses a proven sibling pattern (naba) rather than inventing infra; `workflow_run` chain off "Release" correctly extends the cargo-dist-autogenerated `release.yml` without editing it.
- Clean author-only / go-live split: Epics 1–6 produce no cloud state; all billable/irreversible AWS work sits behind a capability gate with a machine-checkable `Test:`.
- Install-default drift risk (README advertises curl|sh before the domain is live) is explicitly acknowledged and tied to 7.4 end-to-end verification.
- Skill-page auto-generation from SKILL.md frontmatter is the correct anti-drift design.

## Concerns

| # | Severity | Concern | Recommendation |
|:--|:---------|:--------|:---------------|
| C1 | medium | Epic 7.1 sequencing contradiction: 7.1 sets the distribution-id secret (known only after 7.2 provision) and the OIDC role ARN (role created in 7.3), yet runs before both. | Split 7.1: set pre-known secrets (domain, hosted-zone id, GA id) up front; set the distribution-id secret as part of 7.2's completion; ARN after 7.3. |
| C2 | medium | No coarse plan-031 tracking issue: Upstream table lists only the superseded #28/#54; AGENTS.md mandates ONE coarse tracking issue per plan. | Ensure the coarse plan-031 tracking issue is filed (handled by yf-plan INTAKE §4.5; noted explicitly in the plan). |
| C3 | low | Epic 2.1 misstates frontmatter fields ("name, description, triggers, group"). Actual fields: `name`, `description` (trigger text is prose inside `description`), `skill-group`; no `triggers`/`group`. | Correct 2.1 to read `name`, `description`, `skill-group`, parsing TRIGGER/SKIP prose out of `description`. |
| C4 | low | PR-must-not-deploy guard not called out: naba's `web-deploy.yml` guards the `workflow_run` path with `event == 'push' && conclusion == 'success'` because `release.yml` also runs on `pull_request`. | Make the `event == 'push'` guard an explicit acceptance detail of 6.2. |

## Missing

- Reconcile step creating the coarse plan-031 tracking issue (see C2) — resolved via INTAKE §4.5.
- SPEC-first is correctly N/A (docs/site/infra, no skill-behavior `SPEC.md` change).
- No explicit success criterion asserting the teardown/rollback path works (low priority; go-live verified, un-provisioning not).

## Gate Assessment

Gates are appropriately scarce and correctly placed. The AWS capability gate has a real machine-checkable `Test:` and blocks exactly the billable beads (7.2/7.3/7.4) while leaving author-only work ungated. Reconcile Gate (auto) is standard. Resolving C1 also cleans up the 7.1/gate entanglement.

## Upstream Assessment

Dispositions sound. Both #28 and #54 are genuinely **supersede** (generator Docusaurus→Pelican, hosting GitHub Pages→S3/CloudFront), each mapped to concrete resolving epics. Gap: the missing coarse plan-031 tracker (C2), added at intake.

## Operator Resolutions

| # | Concern | Resolution | Status |
|:--|:--------|:-----------|:-------|
| C1 | 7.1 sequencing | Split Epic 7 secret-setting: 7.1 sets only pre-known secrets; new 7.2a sets distribution-id secret post-provision; ARN moved after role creation. Plan updated. | resolved |
| C2 | No coarse tracker | Added explicit note in Upstream Issues section that INTAKE §4.5 files the coarse `plan-031 execution tracking` issue per AGENTS.md convention. | resolved |
| C3 | 2.1 field names | Corrected Issue 2.1 to `name`, `description`, `skill-group`; parse TRIGGER/SKIP prose from `description`. Plan updated. | resolved |
| C4 | PR-no-deploy guard | Added `event == 'push' && conclusion == 'success'` guard as explicit acceptance detail of Issue 6.2. Plan updated. | resolved |
