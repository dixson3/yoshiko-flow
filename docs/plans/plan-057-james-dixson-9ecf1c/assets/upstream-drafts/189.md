---
type: Asset
okf_spec: OKF-PLAN
description: "Draft reconcile comment for upstream #189, PARKED — awaiting an operator grant to post."
---
# Draft comment — #189 (Six shipped scripts have no tests at all)

**Disposition: `partial`. #189 stays OPEN — the original six are untouched.**

plan-057 took #189 as a **constraint** (D-8) rather than as work: the new `yf-okf-hygiene` skill
ships **with** its tests, so it does not become a seventh untested script.

- `skills/yf-okf-hygiene/scripts/test_okf_hygiene.py` — 17 tests, fixture-driven.
- Wired into `CHANGE-VALIDATION.md` in **both** tiers (`okf-hygiene-tests`), so it runs on every
  land rather than once.
- The file carries a PEP 723 `dependencies` block, `import pytest`, **and** an `__main__` runner
  block. All three are load-bearing and were measured: without the runner, `uv run <file>` merely
  IMPORTS the module, executes no test, and exits **0** — verified by appending `assert False` to
  a copy (exit 1 with the runner, 0 without). 36 of 74 test files in this repo carry no
  `__main__` block, so this is a coin flip rather than a formality.

**What remains:** the six scripts #189 actually names still have no tests. This comment records
that the population did not grow, not that the debt was paid.
