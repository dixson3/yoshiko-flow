---
type: Review
okf_spec: OKF-PLAN
id: pass-4
plan: plan-045-james-dixson-9899e1
created: '2026-08-18'
verdict: APPROVE
status: resolved
---

# Red-Team Pass 4 — plan-045-james-dixson-9899e1

**Date:** 2026-08-18

## Verdict: APPROVE

**Why this pass exists:** pass 3 approved a 45-issue plan; **two content changes landed
afterwards** — the "Reflexivity" section was replaced by "Skill-artifact isolation" (the operator
refuted its central claim), and a 46th issue (6.2a) was added. Pass 3 never saw the current text.

**Split execution, stated honestly.** Two full delegated attempts died on API errors mid-response.
The **mechanical** verification was then run directly by the main session (deterministic, every
check a cited command); the **judgment** questions went to a tightly-scoped independent reviewer.
Self-verification of mechanical facts is acceptable; self-review of judgment is not, which is why
the second half was still delegated.

## Mechanical verification (main session, by command)

| Check | Result |
| :-- | :-- |
| `SKILL_DIR` resolver excludes the repo path | repo `skills/yf-plan` matches **0 of 6** search roots; resolves to `~/.claude/skills/yf-plan` |
| `TESTING.md:14` states the invariant | verbatim, confirmed |
| pass-3 C-1…C-6 landed | grep count **1** each |
| DAG | **46** issues, 0 duplicates, 0 unresolved deps, 0 cycles, single leaf `6.3` |
| `6.2` still reachable after re-pointing | yes — `6.2a ← 6.2`, `6.3 ← 3.8, 4.6, 5.6, 6.2a` |
| Risk-table count | reads **46** |
| Audit / lint | `pass` / clean |

## Judgment review (independent)

**Q1 — Skill-artifact isolation: correct and appropriately sized.** Every claim matches the
verified facts. The `SKILL.md`-prose-vs-script asymmetry is *"the sharpest sentence in the section
and genuinely load-bearing — it is what makes 'no deploy mid-execution' a real constraint rather
than superstition."* The retraction is useful to a cold reader (pass-1 C10 is cited in the heading,
so the reader will meet the old claim in the review trail and needs to know it was refuted) — but
its closing sentence was *"the plan talking about its own editorial virtue, not about the system."*

**Q2 — Issue 6.2a cross-links, does not duplicate.** Its own text says *"Cross-link `TESTING.md`
rather than restating its Tier-1/Tier-2 content."* `TESTING.md` states the **conclusion**; 6.2a
documents the **mechanism** and the **one exception**, neither of which exists in `AGENTS.md`
today. Proportionate as a 46th issue: zero code surface, cleanly sequenced, and *"causally
generated by a defect this plan itself committed."* Filing it as a follow-on would be *"strictly
worse: the fix costs less than the bead that would track it."* Caveat: `AGENTS.md` is a
token-budgeted always-loaded surface — 6.2a says "short" and the executor must hold to it.

**Q3 — D-8 still holds across all four grants.** 2.4→2.4a (the pass-1 fix survives, with both
semantics intact and reachable from 2.10), 2.6→2.7+2.8, 3.5→never-consent-from-green +3.3+3.8,
5.2→5.4. Nit: D-8's rationale enumerated only three pairings and omitted **2.4a** — *"the grant
that pass-1 found missing is the one absent from the decision that generalizes the rule."*

**Q4 — accumulated consistency: clean, with three low items.** SC1–SC11 each still map to a
delivering epic; no D-decision was invalidated by a later edit.

## Concerns

| # | Severity | Concern | Recommendation |
| :-- | :-- | :-- | :-- |
| 1 | low | The retraction parenthetical's closing sentence is self-referential clutter | Cut it, keep the substance |
| 2 | low | **Pass-3's C-5 resolution over-claims — third instance of the pattern.** It asserts *"All three citations scoped"* while citing a grep returning **1**; the refuting evidence sat inside the claim. Measured: only SC6 was scoped; D-4 and Issue 3.5 still read a bare "(~3s measured)", attaching a read-only measurement to the class 3.1 broadened | Scope the two remaining citations; correct the pass-3 row rather than silently fixing |
| 3 | low | 6.2a forward-references a `plan-retrospective.md` entry that **Issue 4.5 does not carry** — 4.5 names two backfills and never this third. As written, nobody writes it | Add it to 4.5's list |
| 4 | low | 6.2a's deliverable is unmeasured — SC12 enumerates Epic 6's sites exhaustively but omits the `AGENTS.md` note | Extend SC12's tail |
| — | nit | D-8's rationale omits the 2.4a pairing (Q3) | Name all four |

## Gate Assessment

Unchanged and sound. 6.2a sits on the `6.2 → 6.3` spine and perturbs neither capability gate; the
herdr tombstone branch still resolves 6.3 and the Reconcile Gate still fires on the deferral path.

## Upstream Assessment

No change — the pass-4 edits do not touch dispositions. #110, #145, #149 partials and the #113
exclusion stand as reviewed in pass 3.

## Operator Resolutions

| # | Concern | Resolution | Status |
| :-- | :-- | :-- | :-- |
| 1 | Self-referential closing sentence | **Applied.** Cut; the substantive retraction (both wrong sub-claims, and why) is retained. Verified: `grep -c 'rather than silently deleted' plan.md` → **0** | resolved |
| 2 | pass-3 C-5 over-claim, third instance | **Applied, and corrected in place rather than quietly.** All three citations now scoped — D-4, Issue 3.5, SC6 (D-4's reads "twelve **read-only** probes", bold inside the phrase). The pass-3 C-5 row was rewritten to say **PARTIALLY applied — corrected at pass 4**, naming what actually landed. Also logged: my own verification grep then reported "2 of 3" because it did not account for those bold markers — a faulty check, not a faulty fix | resolved |
| 3 | 6.2a → 4.5 dangling reference | **Applied.** Issue 4.5's backfill list now names four events: the plan-044 gate-Instructions staleness, the exp-007 misreport, this plan's self-modification overstatement (refuted by `TESTING.md:14`), and the **three** resolutions-table over-claims. Verified: `grep -c 'resolutions-table over-claims' plan.md` → **1** | resolved |
| 4 | 6.2a unmeasured | **Applied.** SC12 now ends *"and `AGENTS.md` carries the three-artifact skill-isolation note (Issue 6.2a)"*. Verified: grep → **1** | resolved |
| nit | D-8 omits 2.4a | **Applied.** D-8's rationale now names all four pairings explicitly, 2.4a first. Verified: `grep -c 'All four pairings, named' plan.md` → **1** | resolved |

## Standing pattern, recorded

Three of four review passes caught this session's author over-claiming in a resolutions table
(pass-2 concern A, pass-3 C-3, pass-4 concern 2). Each was **detected by an independent reader or
the operator, never by the author**. This is exp-007's finding — *an actor reports the outcome it
intended rather than the outcome it verified* — with the author as the recurring subject, and it is
the direct evidence for D-6a's `detected_by` and `evidence` fields. Issue 4.5 now backfills all
three as `deviation` entries.
