---
type: Review
okf_spec: OKF-PLAN
id: pass-4
description: Red-team pass 4 (fourth independent) — REVISE, 1 high / 3 medium / 3 low; converging, and RE-002 named against the review process itself
---

# Red-team pass 4

## Verdict: REVISE

1 high, 3 medium, 3 low. **ALL 7 RESOLVED** (see Resolutions), plus the Missing item on `ownership-report` recorded as **R11, an ACCEPTED residual risk**. **Concern count 23 → 12 → 12 → 7**, and **4 of the 7 share one root cause
with one fix**. Defects-inside-the-previous-fix fell from **9-in-12 to 2-in-12**.

## Strengths (all verified by execution)

- **Every mechanical property re-verified clean after the fourth rewrite** — 31 issues, 49 edges,
  36 criteria, 5 gates, `unparsed: []`, acyclic, 0 unknown edge nodes, 0 dangling `Discharged-by`,
  0 issues discharging no criterion, **31/31 declare `touches:`**. `audit` exit 0 (every finding
  report-only); `doc_lint` PASS 0/0.
- **P3-C1's floor genuinely closes pass-3's hole** — spiked: `empty → 2`, `unreadable → 2`. Because
  the floor sits *in front of* the closure control, an empty set is INCONCLUSIVE rather than a green
  SC0/SC0b/SC2. **RE-002's remedy correctly applied.**
- **Control closure is perfect and generated** — 29 asserted, 29 built, 0 either direction, **0
  controls with >1 builder**. Sets derive cleanly from builder epic: core 21 / ext 4 / land 4.
- **P3-C4's diagnosis verified exactly** — `plan_manager.py` writers = **5**. R10's restated figure
  is correct.
- **P3-C9 verified** — 0 `uv run` on shell, 34 `bash` sites, and the Python-control criteria
  correctly retain `uv run`. **P3-C7 verified** — `heavy` = 0 occurrences.
- **Both gates re-verified sound on both arms** — `core`: behind-set 16, **violations NONE**;
  `ext`: behind-set 9, **NONE**.

## Concerns

| # | Severity | Concern |
| :-- | :-- | :-- |
| P4-C1 | **high** | **~21 of 29 controls have no reachable exit-1 RED, and P3-C3's fix now hard-refuses their only honest verdict.** The exit-1 rule was written into **3.1 alone**. Every other builder observes an interface shipped by a later issue, so it gets argparse **exit 2** — `ctl-199b-*` ×5 (2.2), `ctl-113-gate` (4.2), the ext four, the land four, `ctl-ownership-inconclusive` (1.5). Three others (`ctl-spec-first-order`, `ctl-class-a-fraction`, `ctl-touches-coverage`) are **GREEN at their builder and cannot be RED at all**. Both gates are unsatisfiable for the majority of controls they gate |
| P4-C2 | med | **The RED-observation record has no declared path and no declared writer.** Both Conditions and SC2 require *"a **recorded** RED observation"*; no artifact exists. Either the ledger is load-bearing with no writer (P3-C4's class, uncaught), or `verify-all` is a presence check and **SC2's verification does not assert SC2's prose** (P3-C6's shape at the flagship criterion) |
| P4-C3 | med | **`ctl-empty-set-floor` needs a controls-file override that 0.2 does not commission.** The spike worked *only* because the reviewer added a `CTL_TXT` override. Without it the control cannot construct an empty set to observe. `--fixture`/SC11b again, inside the fix that closed P3-C1 |
| P4-C4 | med | **`ctl-spec-first-order`'s predicate is false by construction.** 0.1's `touches:` are all three under `skills/**`, so the `REQ-*` commit **is** the first `skills/**` commit — the predicate is trivially self-satisfied, never the property SC1c claims |
| P4-C5 | low | **R8's severing recipe is incomplete — a fourth edit is required.** Simulated: severing leaves `SC0`, `SC0b`, `SC2` with dangling `Discharged-by` on 5.0/6.0, which `doc_lint` R1 enforces. Severing as documented would fail the lint |
| P4-C6 | low | **The P3-C4 test-file fix covered 3 of 4 modules** — 5.3's `retrospective_fields.py` has no test file, though the resolution text claims it does |
| P4-C7 | low | **Textual residuals** — 0.3 reads "build its **two** controls" while listing three; SC6b's text still says "bounds recursion at depth 1", the phrasing P3-C8 replaced (the corrected form landed in 2.2 only); SC1c is ordered before SC1b |

## Missing

- **A harness-level contract in 0.2**: the RED-observation record's path and writer, the
  controls-file override, the uncommissioned-interface→exit-1 rule, and how `gate-run.sh` invokes
  each `ctl-*.sh`. P3-C9 moved the lost-exec-bit hazard **down one level** — the criteria use
  `bash gate-run.sh`, but nothing says the dispatcher invokes controls as `bash "$ctl"`.
- **Any criterion forcing action on `ownership-report`'s finding.** Measured: **all 10 pairs** of the
  5 `plan_manager.py` writers are topologically INDEPENDENT — the plan's own 2.86× lever firing at
  maximum on its own DAG. Honest deliberate acceptance, but it should be stated as an **accepted
  residual risk**, since the report is generated by one of the five writers.

## Gate Assessment

| Gate | Reachable? | Verdict |
| :-- | :-- | :-- |
| `red-prework-core` | **Structurally yes** (behind-set 16, violations NONE) | **Blocked by P4-C1** — demands exit 1; ~17 of its 21 controls can only produce exit 2 or green at their builder |
| `red-prework-ext` | **Structurally yes** (behind-set 9, NONE) | Same P4-C1 defect on all 4 ext controls |
| `upstream-write` | **Yes — re-verified** | Sound |
| Reconcile Gate | **Yes** | Sound; exclusion intact, non-self-blocking |

## Upstream Assessment

Unchanged and sound. `verify-reconcile` returns `verdict: fail` pre-execution with per-row detail —
the correct RED. 22 rows; every `include` names a resolving issue; #194/#177 correctly claim none.

## Convergence assessment

**Converging — and RE-002 applies to the REVIEW PROCESS, not to any one defect.**

> Four successive passes have been refuted the same way: **a global property is repaired at the one
> site the reviewer named.** P3-C3 demanded exit-1 REDs and got the rule written into 3.1 alone.
> P3-C4 demanded call sites and tests and got 3 of 4. P3-C1 demanded a floor and got one without the
> override it needs. P3-C12 demanded a pre-merge order check and got one whose glob its own SPEC
> issue violates. In every case the *named instance* was fixed and the *class* was not swept.

**The check to put in front of the failing component:** widen `ctl-controls-closure` to
**`ctl-harness-contract`** — *every file `gate-run.sh` reads or writes, and every external interface
any `ctl-*.sh` invokes, must appear in some issue's `touches:` or be demonstrated to exist on the
current tree.* Mechanical, and it would have caught P3-C4, SC11b's `--fixture`, P4-C2's ledger and
P4-C3's override **before a reviewer had to name each one**.

**On severing Epics 5 and 6: NOT indicated.** The defect lives in the Epic 0/1/2 harness, not in
5/6 — severing removes 6 issues and 4 criteria without touching the root cause. Epics 5–6 are also
the cheapest work in the plan, and R8's recipe is measurably incomplete (P4-C5).

## Resolutions

| Concern | Severity | Resolution | Actor | Status |
| :-- | :-- | :-- | :-- | :-- |
| P4-C1 | high | **Fixed as a CLASS, in one place — which is your whole diagnosis.** The uncommissioned-interface rule is hoisted out of 3.1 and into **0.2's harness contract**, stated once: *a control observing an interface a later issue ships MUST map that observation to exit 1 (a real negative); the callee's argparse exit 2 must never escape. Exit 2 is reserved for the instrument itself failing.* The three **genuinely-green-at-builder** controls you identified now get pinned negative fixtures, exactly as 3.1 already does — `ctl-spec-first-order` (0.3, a recorded history where impl precedes SPEC), `ctl-class-a-fraction` (1.1, a prose-cell fixture), `ctl-touches-coverage` (1.3, a `touches:`-less issue). Both gates are now satisfiable for every control they gate | `main-session` | `resolved` |
| P4-C2 | med | **Fixed — the ledger is named, with a declared writer and reader.** 0.2's contract now specifies `assets/red-observations.tsv`, **APPENDED BY `gate-run.sh` on every `run`** and **READ by `verify-all`/`verify-set`**, and it is in 0.2's `touches:`. That resolves the dilemma you posed: `verify-all` is not a presence check, so SC2's verification now does assert SC2's prose | `main-session` | `resolved` |
| P4-C3 | med | **Fixed — the override is commissioned.** 0.2's contract declares `CTL_TXT` (env) so a control can point the dispatcher at an alternative set. Without it `ctl-empty-set-floor` could not construct an empty set to observe, which is the `--fixture`/SC11b shape recurring inside the fix that closed P3-C1 | `main-session` | `resolved` |
| P4-C4 | med | **Fixed — you were right that it was false by construction.** The predicate is now *the commit touching `skills/*/spec/**` or `skills/*/SPEC.md` precedes the first commit touching any OTHER `skills/**` path*, and SC1c records that the earlier form was self-satisfied because 0.1's own touches are all under `skills/**`. The check is now stated as **PRE-MERGE and PRE-SQUASH at 7.1** | `main-session` | `resolved` |
| P4-C5 | low | **Fixed.** R8 now says severing needs **FOUR** edits, not three, and cites your simulation: the documented recipe fails `doc_lint` R1 because P3-C2's fix added 5.0/6.0 to SC0/SC0b/SC2's `Discharged-by`. The fourth edit removes them | `main-session` | `resolved` |
| P4-C6 | low | **Fixed.** `skills/yf-plan/scripts/test_retrospective_fields.py` added to 5.3's `touches:`. The P3-C4 resolution text claimed all four modules got test files and that was false for 5.3 — a resolution that over-stated what it verified, which is its own instance of the class | `main-session` | `resolved` |
| P4-C7 | low | **All three fixed.** 0.3 now reads "its THREE controls" and names why the third needs a fixture; SC6b's text now carries the corrected depth rule (*depth 0 and 1 evaluate; depth 2 returns exit 2 without executing*) rather than the phrasing P3-C8 replaced; SC1c is reordered after SC1b | `main-session` | `resolved` |
