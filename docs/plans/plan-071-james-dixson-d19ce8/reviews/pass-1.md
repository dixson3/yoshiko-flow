---
type: Review
okf_spec: OKF-PLAN
description: "[red-team pass 1, reading] REVISE — D-10 premise false (2 ci-release plans exist), 4.4 contradicts 4.1, gate-consistency halt would fire post-push on gate-less plans; 13 concerns, 3 high, all measured"
plan: plan-071-james-dixson-d19ce8
pass: 1
mode: reading
---
# Plan Red-Team: plan-071-james-dixson-d19ce8

## Verdict: REVISE
**Mode:** reading

## Strengths
- The two findings files are genuinely measured, and the plan's decisions are traceable to them; the SPEC-first ordering (every epic hangs off Epic 0) holds by construction.
- The pre-review smoke of all 20 SC rows and the tightening of three pre-green rows is the right instinct and already caught the class this plan is about.
- The REQ-LAND prune is a compression map, not a deletion list; every retained id keeps a named test.
- D-10 was explicitly opened to challenge — and the challenge lands (C1).

## Concerns
| # | Severity | Concern | Recommendation |
| :-- | :-- | :-- | :-- |
| C1 | high | **D-10's premise is false and its zero-hit branch is unreachable by construction.** measured: `grep -l 'deliverable_class: ci-release' docs/plans/*/plan.md` → 4 files: plan-031 and plan-041 carry `deliverable_class: ci-release` as frontmatter (line 8), plan-039 matches only in prose, and **plan-071's own plan.md** matches because Issue 4.2 quotes the needle. The subsystem *has* been used, and Issue 4.2's measurement can never return zero while this plan text exists. | Rewrite D-10/4.2/0.3 now: the subsystem stays; drop the "conditional retirement" of REQ-PLAN-069/069a; scope 4.2 to repairing `attest-validation`'s dead offer path only. Recount SC1 (see C4). Never embed a grep needle in the document the grep scans — anchor on frontmatter (`^deliverable_class: ci-release$`) or exclude the plan's own dir. |
| C2 | high | **Issue 4.4 contradicts Issue 4.1 and D-2.** measured: 4.1 deletes the `gate-consistency` verb; 4.4 "adds it to `LAND_CLOSE_CHAIN` as halting"; `_land_l8_to_l15_close_chain` (plan_manager.py:9942) builds `["uv","run",me,verb,…]` — the chain can only run pm verbs. Either the verb survives (then it is not dead and SC15 must drop it) or a new chain step is written (forbidden by D-2's "no new land step"). | Pick one: keep the `gate-consistency` wrapper verb (remove it from 4.1/SC15, leaving 7 dead verbs) and wire it as the halting row. Recompute SC1's ceiling accordingly. |
| C3 | high | **D-8 + 4.4 makes every gate-less plan halt after the irreversible push.** measured: `gate_consistency.py` returns `gates:0` when no capability gate has an issue-kind `Blocks` member (EXP-002: 54/99 gates skipped; plans 065/069/071 PASS with zero evaluable). 4.4 turns zero → exit 2; D-8 turns exit 2 on a halting chain verb into a halt; the chain runs at L8, **after L6's push**. plan-069 has 0 capability gates (`grep -c '^### Capability Gate'`). A "zero gates" plan is legitimate, not inconclusive. | Distinguish "no gates declared" (PASS, gates:0, exit 0, a fact) from "gates declared but none evaluable" (exit 2). Run `gate_consistency.py` at `ready-check` (pre-approval, pre-push), not only at L8; then the L8 row is a regression guard, not the first look. |
| C4 | medium-high | **SC1's ceiling is at the arithmetic edge and breaks under C1/C2.** measured: 41 verbs − 8 dead − `audit-close` − `parked` − `judgement-never-fired-report` = **30** exactly. With D-10 not firing (C1) nothing else goes; with `gate-consistency` retained (C2) the count is 31 and SC1 is red on a correct tree. | Set the ceiling from the resolved deletion list (≤31 or ≤30 after deciding C2), and state the arithmetic in the plan so an executor can see slack of zero. |
| C5 | medium-high | **`halts_post_irreversible` is derived from an artifact the landing deletes and that never records the halts it counts.** measured: `LandingJournal.write` only fires when a step returns `journal` (plan_manager.py ~10621); halting fails at L9/L10/L11 return `journal=None`, so no "halt state transition" exists except the four `L_CONFLICT_*` and two `L_REJECTED_PUSH_*` states; `ctx.journal.clear()` runs at the terminal green state (:10639); `/.yf/` is gitignored (.gitignore:18); today only `plan-062…json` survives in `.yf/plan/landing-journal/`. Issue 1.2 as written derives zero for any landing that eventually completes and `no-journal` for all of 060–070 (Issue 1.3's baseline is therefore empty on its second number). | Redefine the source: have `land --apply` append each halting `fail` step (verb, journal phase, `irreversible` flag) to a persisted, in-bundle record (e.g. `log.md` or `assets/landing-halts.json`) *before* the journal is cleared, and derive the number from that. Keep `no-journal` for pre-change plans. The first half (`sc_flipped_post_approval`) *is* derivable: 48 `INTAKE approved` commits exist (`git log --grep`), so `git show <sha>:plan.md` works. |
| C6 | medium-high | **R1 understates the plan-070 collision, and D-5 is violated by Issue 3.2.** measured: plan-070 Issues 0.2/0.5/0.6 add `REQ-LAND-039` and amend 004/006/015/017a in `spec/landing.md`; 071 Issue 0.4 rewrites that file wholesale — a guaranteed conflict in either order, and 039 pushes the id count to 23 vs SC2's ≤22. plan-070 Epic 3 (Issues 3.1/3.2) implements the `route_record` writer *against* `_land_route_record_findings` and ships a negative control that the reader fires; 071 Issue 3.2's "otherwise" branch deletes that reader. If 071 lands first, 070's Epic 3 is invalidated — that is "touching plan-070". | Make land-after-070 the rule, not "where possible": add a capability gate on `git log main --grep='plan-070' --oneline` non-empty blocking 0.4 and 3.2, or drop 3.2's delete branch entirely and narrow 015 only. Give 039 a destination row in the 0.4 merge map (e.g. into the 004 L6 row) so SC2's ceiling survives. |
| C7 | medium | **Issue 4.3's 30s per-criterion timeout silently un-evaluates SC14 at L11.** measured: recheck's `TimeoutExpired` → `status: inconclusive`, "never counted as evaluated" (plan_manager.py:3385–3405); FULL tier is documented as multi-minute. SC14 will be inconclusive at every L11 run — the exact vacuous class — while the verdict stays PASS/FAIL on the other rows. | Either mark SC14 `manual:` with the 5.1 log line as evidence, or give `recheck-criteria` a per-row `timeout:` override and set it on SC14. Same check for any `uv run test_*.py` row that cold-starts >30s. |
| C8 | medium | **SC11's grep is mis-specified for the shape the plan will produce.** measured: `grep -c '"gate-consistency".*halting=True\|gate_consistency.*True'` → 0 today; a `LAND_CLOSE_CHAIN` row is a positional tuple `("gate-consistency", "l8_…", True)` — hyphenated, no `halting=` keyword — so leg 1 cannot match; leg 2 needs the underscore form and will match any docstring line containing `gate_consistency … True`. | Assert the tuple: `grep -q '("gate-consistency", *"l8_[a-z_]*", *True)'`, or better, a test in `test_land_apply.py` that reads `LAND_CLOSE_CHAIN` and asserts the halting flag. |
| C9 | medium | **SC12 as written forces deletion of helper modules or false-fails.** measured: today's loop prints UNWIRED for `_common.sh` (sourced by the `.sh` checks), and `scripts/checks/*` includes `__pycache__`, `_figures.py`, `_web_corpus.py` (imported, not named). Issue 4.5 says delete "every file not named" — that deletes the helpers the surviving checks import. | Exclude `_*` and `__pycache__` from both the SC12 glob and 4.5's deletion set; or wire helpers via the importing check ("named or imported by a wired file"). |
| C10 | medium | **D-9's freeze check has a vacuity mode R7 does not name.** inferred: "≥1 `test_*.py` reference" is a string-mention test; a test that merely lists verbs (`test_cli_enumeration.py` enumerates all 41) satisfies it for every verb, including the 8 dead ones, so leg (b) of D-1 ("a fixture-failing test") is not what the check measures. | Require the reference in a test that *invokes* the verb (`plan_manager.py <verb>` in an argv or `CliRunner.invoke`), and add a fifth negative control: a verb whose only test hit is an enumeration list must fail. |
| C11 | medium | **Issue 1.2 assumes "the latest `recheck-criteria` output" — no predecessor persists one.** measured: L11's output lives only in the `land --apply` result; nothing writes it to the bundle (`grep -n 'land-beads.json\|landing-report' plan_manager.py` finds only the beads file). | Have 1.2 run `recheck-criteria --json` itself (with the preamble env, per #364) rather than read a file no step writes. |
| C12 | low-medium | **R4/D-8's effect on plan-070 is stated but the mitigation costs the metric.** measured: plan-070 is `ready-for-approval` with 15 prose-form SC rows (0/15 evaluable, EXP-002); "migrate 070's criteria to clause form" post-approval is, by D-4's own definition, 15 `sc_flipped_post_approval` events. inferred: the halt is only reachable after 071 is *deployed*, so ordering 070's landing first removes the hazard for free. | State the order explicitly (see C6) and note that the alternative counts as flips. |
| C13 | low | **Two reading passes + one execution pass** — this pass found C1–C3 with three greps, which supports the plan's thesis that measurement beats reading; but the execution pass runs the *installed* red-team brief, which has no `Mode` rule, so SC9 depends on the main session hand-writing the `**Mode:**` line. | Note in Issue 2.1/SC9 that pass 3 for *this* plan is driven by an explicit brief, and record that in `log.md` so SC9 is not a phantom. |

## Missing
- No issue runs `gate_consistency.py` at `ready-check`/intake, though Motivation names it as the detector plan-068 landed past (4 findings).
- Issue 0.4's merge map has no destination for `REQ-LAND-039` (plan-070) — see C6.
- `grep` under `bash -c` on this host resolves to **ugrep 7.8.4** (`grep --version`); SC8's `\|` alternation passed here but the plan's "identical in both shells" claim says nothing about the grep implementation. Low; note it in context.md.
- Issue 1.3 should say what "source" is for 060–070 given C5 (all `no-journal` but 062).

## Gate Assessment
- **Start gate:** fine.
- **"allocated REQ ids are free":** measured — REQ-AGENT-066, REQ-PLAN-084/085/086 have zero hits; highest existing are AGENT-065 and PLAN-083. Reachable, ONE-SHOT correctly declared, no cycle with its `Blocks` set. Consider also testing `REQ-LAND-039` is *not* free after 070 lands, to drive the C6 branch.
- **"baseline suite green":** reachable, correctly `build` class. It blocks 3.1/4.1/4.3 but not 2.5 or 4.5, which also delete; extend `Blocks` to every deleting issue. A gate for "plan-070 has landed" (C6) is the frontloading miss: its evidence exists before execution and it would settle 0.4, 3.2 and SC2 up front.
- **Reconcile gate:** the #388 instance, acknowledged in R5.

## Upstream Assessment
Dispositions are reasonable and the partials are specific. Two adjustments: **#325** (`include`, resolved by 4.4) must be re-scoped per C3 — the fix as written creates a new post-push halt class rather than removing a vacuous PASS; and **#392**'s "In" list should name the D-10 finding (C1) as another instance of the class, since the plan's own measurement was declared in prose and never run before approval. #289/#328/#312/#395 excludes are justified; #384's split (defect 2 in, defect 1 out) is clean.

## Resolutions

**Status: all 13 resolved by the main session on 2026-09-10; pass 2 verifies.** Missing-section items: gate_consistency at ready-check → 2.4; REQ-LAND-039 destination → 0.4; ugrep note → context.md; 1.3 source column → 1.3. Gate items: baseline gate Blocks extended to 2.5/4.5; plan-070 gate added. Upstream: #325 and #392 rows updated.

| Concern | Severity | Resolution | Actor | Status |
| :-- | :-- | :-- | :-- | :-- |
| C1 D-10's premise is false and its zero-hit branch is unreachable by construction | high | D-10 withdrawn: subsystem retained; 4.2 re-scoped to a test for `attest-validation`; 0.3 drops the conditional retirement; grep-needle rule added to D-10 text; #392 In-list records the instance | `main-session` | `resolved` |
| C2 Issue 4 | high | `gate-consistency` verb kept as the chain's engine runner; removed from 4.1 and SC15 (7 dead verbs) | `main-session` | `resolved` |
| C3 D-8 + 4 | high | 4.4 now separates no-gates (PASS, exit 0) from none-evaluable (exit 2); 2.4 runs the engine at `ready-check` pre-approval; the L8 row is a regression guard | `main-session` | `resolved` |
| C4 SC1's ceiling is at the arithmetic edge and breaks under C1/C2 | medium-high | SC1 ceiling set to 31 with the arithmetic stated in SC1 and in REQ-PLAN-086 (Issue 0.3) | `main-session` | `resolved` |
| C5 `halts_post_irreversible` is derived from an artifact the landing deletes and that never r | medium-high | Source redefined: `land --apply` writes `- landing-halt: <phase> <verb>` to `log.md` before the journal is cleared; D-4 and 1.2 rewritten; 1.3 records `no-record` and a `source` column | `main-session` | `resolved` |
| C6 R1 understates the plan-070 collision, and D-5 is violated by Issue 3 | medium-high | New `probe` gate 'plan-070 has landed' blocks 0.4 and 3.2; D-5 revised to land-after-070; 0.4 folds REQ-LAND-039 into the merged 004 L6 row and carries 070's amendments; 3.2's delete branch removed (reader relocated because 4.3 deletes `audit-close`); R1 rewritten | `main-session` | `resolved` |
| C7 Issue 4 | medium | SC14 is `manual:` with SC14b as the log-evidence clause; 4.3's default timeout set to 60s | `main-session` | `resolved` |
| C8 SC11's grep is mis-specified for the shape the plan will produce | medium | SC11 asserts via `test_close_contract.py` reading `LAND_CLOSE_CHAIN` (4.7), not a grep on source | `main-session` | `resolved` |
| C9 SC12 as written forces deletion of helper modules or false-fails | medium | 4.5 and SC12 exclude `_*`/`__pycache__` and treat 'imported/sourced by a wired file' as wired | `main-session` | `resolved` |
| C10 D-9's freeze check has a vacuity mode R7 does not name | medium | 4.6 and D-9 require an invocation-form test reference; fifth negative control (enumeration-only hit) added; R7 updated | `main-session` | `resolved` |
| C11 Issue 1 | medium | 1.2 runs `recheck-criteria --json` itself with the preamble env | `main-session` | `resolved` |
| C12 R4/D-8's effect on plan-070 is stated but the mitigation costs the metric | low-medium | Folded into C6: ordering removes the hazard; R4 downgraded to low and notes that migrating 070's criteria would count as flips | `main-session` | `resolved` |
| C13 Two reading passes + one execution pass — this pass found C1–C3 with three greps, which su | low | SC9 text notes pass 3 is driven by an explicit brief recorded in `log.md` | `main-session` | `resolved` |
