plan-049-james-dixson-725bc0 has landed. The enforcement binding closes more of this class; the
falsification pass itself stays open.

What now exists that did not:

- **Every criterion in this plan was driven, and two were driven to a documented MISS** rather
  than quietly restated. SC31/SC23's residue targets were measured at 83 and 75 against ≤81 and
  ≤73, with the derivation of why the target was unreachable published alongside.
- **Controls are required to FAIL before they are trusted.** The DAG guard is gated on failing
  two named mutants and passing a real 49-bundle migration; both capability-gate scripts were
  observed **RED at exit 1** before their capability existed.
- **Claims are cross-checked against the code that scores them.** The check that "a count-only
  implementation would pass mutant B" is implemented as a straw man **in the test suite**, so it
  is a measurement rather than a prediction — if a future refactor makes the real guard
  count-based, the two will agree and the assertion will fail.

Still open, and this issue's remaining substance: a review-phase pass that systematically
attempts to **falsify** each criterion before approval. Everything above is execution-time
evidence; none of it runs while the plan is still being drafted.
