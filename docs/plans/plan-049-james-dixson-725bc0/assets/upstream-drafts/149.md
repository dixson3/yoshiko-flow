plan-049-james-dixson-725bc0 has landed and closes **M5**. **M9 remains open and is explicitly
out of scope**, with its measurement recorded rather than implied.

**M5 — process rules that nothing executes — CLOSED.** The document linter now has an executing
home at three bindings:

- **Intake.** `_audit_plan` folds the linter's verdict into the portability audit, so
  `ready-check` (exit 3) and `audit` (exit 1) gate on it. Driven: an in-flight bundle with an
  injected malformed heading takes `ready-check` from **exit 0 to exit 3**. This is the
  fail-closed gate plan-047's Epic 9 named and nothing ever wired.
- **On edit.** `skills/yf-plan/protocols/DOC-LINT.md`, always-on with **no marker** — inertness
  is structural, because the engine is path-keyed.
- **`CHANGE-VALIDATION.md`.** The gate scripts are promoted to committed §1 rows, driven
  two-sided: with the rows a breaking change yields `status: fail`; with them deleted the
  identical tree yields `{"commands": [], "status": "pass"}` — a vacuous green, never red.

Two blockers had to be cleared first, neither named by the plans that scheduled this work:
the engine **existed in no deployed vault**, and a byte-identical vendor would not have fixed
it — root resolution was positional, so the vendored copy computed its root as the *skill
directory* and returned `files_checked: 0`, which is `verdict: PASS`, exit 0.

**M9 — remediation edges that exist only in prose — OUT OF SCOPE.** Re-measured on the live bead
graph: **26 deduplicated `discovered-from` edges, of which 0 connect two plan epics.** The edge
class M9 describes does not exist in the graph at all, so building it is a different piece of
work from binding a linter. Recorded at
`docs/plans/plan-049-james-dixson-725bc0/assets/epic4-records.md`.
