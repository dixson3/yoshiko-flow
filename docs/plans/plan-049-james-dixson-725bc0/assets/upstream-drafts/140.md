---
type: Reference
okf_spec: OKF-PLAN
id: upstream-draft-140
description: Drafted upstream comment for #140 (partial) — posted at Issue 6.5
---

plan-049-james-dixson-725bc0 has landed and closes the **readability half** of this issue. The
deferred half is unchanged and stays with #171.

**In scope, delivered:**

- **The grammar widening.** `plan_extract` now reads the trailing-inline `depends-on:` form,
  including lettered referents (`A.1`, `B.4`). Measured: **74 declarations recovered across 5
  plans, +100 corpus edges, with ZERO documents modified.** `plan-006` and `plan-007` had been
  reporting `0 unparsed, 0 edges` while carrying declarations between them — the residue metric
  recording the loss as perfection. They now report 9 and 17 edges.
- **The 2-document structural relocation**, under an explicit operator authorization gate:
  `plan-008`'s `Capability Gate: d2 present` block moved into `## Gates` (residue 8 → 3; it does
  **not** clear, and the three remaining refusals are named), and `plan-015`'s `Issue B.3` title
  parenthetical de-bolded (residue 4 → 1, via a 3-row cascade from one edit).

**Out of scope, still #171's:** nested `index.md`/`log.md` generation, the `reindex`/`--fix`
verb, and the drift model.

**Two things worth recording, because they are adverse:**

1. The original scope was **redirected by its own investigation**. The "16 free recoveries"
   re-measured as **7**, all of them one gate block; nine were not gate blocks at all, and
   relocating them would have produced content-empty gates the extractor certifies clean —
   `plan-045`, the only plan the original scope fully unblocked, is *entirely* in that category.
   The dark-matter widening was found to be a larger, unambiguous, zero-write population.
2. A declared residue target was **missed**: corpus `unparsed[]` is **75**, against a derived
   target of ≤73. The +2 is two `plan-010` declarations that were previously **invisible** and
   are now visible-and-refused. Holding the target would have required silently dropping them.
   Recorded in full at `docs/plans/plan-049-james-dixson-725bc0/assets/post-work-measurements.md`.
