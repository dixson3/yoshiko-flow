---
type: Reference
okf_spec: OKF-PLAN
id: upstream-draft-113
description: Drafted upstream comment for #113 (partial) — posted at Issue 6.5
---

plan-049-james-dixson-725bc0 has landed; residue against this issue drops further, and the
topological walk itself remains out of scope.

The blocker this issue keeps running into is that `plan_extract` returned INCONCLUSIVE on 24 of
49 plans, so a walk had nothing complete to walk. That is substantially reduced:

- the trailing-inline `depends-on:` grammar recovers **74 declarations across 5 plans**, +100
  corpus edges, **zero documents modified**;
- `plan-006` and `plan-007` no longer report `0 edges` while carrying declarations;
- the 2-document authorized relocation clears a further 8 residue rows.

Corpus `unparsed[]` is **75**, down from 81 — and the honest caveat is that it went *up* to 83
first, because two previously **invisible** `plan-010` declarations are now visible-and-refused.
That is a strict gain in information and a rise in the count; the two are the same event.

Also relevant to a future walk: `_shared/dag_guard.py` now provides `snapshot`/`verify` over a
four-layer view of the DAG (issues, edges, **raw referent tokens**, gate content) with a 0/1/2
exit contract — a ready-made instrument for asserting that a walk observes what the documents
actually declare.
