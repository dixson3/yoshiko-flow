# checks-allowlist.txt — criterion instruments EXEMPT from the pre-fix RED requirement.
#
# FORMAT: `<check-name.sh>` then whitespace then the REASON, on one line. A bare name with
# no reason is REJECTED by `verify-red-checks` — 0.1 requires the reason to be recorded, and
# an allowlist entry nobody has to justify is not an allowlist.
#
# THE BAR IS HIGH BY DESIGN. Every entry here is a criterion whose instrument is NOT
# evidence-backed by a red->green observation, so each one narrows what the RED gate proves.
# Add an entry only when the check is STRUCTURALLY unbaselineable, never merely because it
# is inconvenient that it passes.

check-harness-smoke.sh  authored by Issue 2.5, which sits DOWNSTREAM of the RED gate, so no pre-fix observation of it can exist at the moment the gate is evaluated — it is structurally unbaselineable rather than exempted. Its red evidence is Issue 6.7's live transcript instead.

check-req-coverage.sh  SC1 is discharged by Issues 0.2-0.5, and the RED gate deliberately does NOT block those (it blocks 1.1, 2.2, 2.4, 3.1-3.4, 3.6). Issue 0.1 depends on 0.7/0.8, so by the time the baseline can be recorded the SPEC amendments have already landed and the criterion legitimately holds. Structurally unbaselineable under the plan's own DAG, not exempted for convenience. Measured green on 2026-08-26 with all four SPEC assertions passing.
