**plan-068 does NOT close this issue.** Disposition `partial` — correction only.

plan-068 (Plan A of the A/B split) published the measured correction to "zero test coverage":
**61.4%** statement coverage of the `--apply` preamble, all of it happy-path plus exactly one
refusal body, with all 17 missed statements being refusal bodies. The **substance holds** — the
refusal paths, including `recover()`'s four-way branch, are the untested part and are the ones
that can re-push.

Also corrected: the bead's six-step preamble list is short by one. The preamble has **nine**
steps; the list maps to 1–7 and omits step 8, `_land_repreview_or_halt`.

**The preamble tests and the bookkeeping guard are plan-069 (Plan B)**, which is the closing plan.
This issue stays **OPEN**, and no plan-068 success criterion asserts its closure — SC10's second
clause asserts the opposite.

One cross-plan coupling to carry forward, corrected against what actually landed: plan-068's
end-to-end `--apply` test drives `_land_execute` **directly** and never reaches `_land_tty_gate`,
because that gate lives in the CLI preamble. So plan-069's `#334` fix cannot break it — but the
thing to verify when that guard lands is that the **CLI-level** `--apply` path remains drivable
under test, since plan-068's coverage deliberately stops at the executor boundary.
