**plan-068 does NOT close this issue.** Disposition `partial` — correction only.

plan-068 (Plan A of the A/B split) published two measured corrections:

- **L4 alone** mutates the excluded facts, not "L4's merge commit and L6's push". L6 pushes to
  `origin`; both facts are read from **local** refs. The premise is confirmed — the mismatch is
  guaranteed rather than conditional — but the attribution doubles the apparent surface.
- **`predicted_tree` is NOT invalidated by the landing's own merge** — measured **stable** across
  L1/L2/L4. This is load-bearing: it is the only covered field stable under the landing's own
  merge *and* unstable under every foreign landing, so a **wide** `merge_preview` exclusion would
  blind the digest to all three foreign-landing classes and let a conflicting landing validate
  `pass`. EXP-003 **confirmed this issue's premise and refuted its proposed remedy.**

**The projection fix is plan-069 (Plan B)**, whose design is not yet settled (plan-068 pass-1
C2/C3). `REQ-LAND-036`'s exclusion-table amendment and `REQ-LAND-018`'s rationale amendment are
also plan-069's; plan-068 Issue 3.3 *verifies* plan-069 carries them rather than performing them.
Bead `yf-jp7z`'s re-scoping is deliberately **held** — re-scoping it to match an unsettled design
would encode a design that does not exist.

This issue stays **OPEN**, and no plan-068 success criterion asserts its closure.
