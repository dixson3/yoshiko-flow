**Re-routed to #317** (Plan 3 of 3) by plan-061's scope split — not addressed here, and staying
open.

The original plan-061 scope was a single combined website/docs realignment. Measured during
investigation it proved too large for one plan and was split three ways:

| | Plan | Tracker |
| :-- | :-- | :-- |
| 1 | README + code-adjacent layout contract, backfill 20 skills | #315 (landed) |
| 2 | `yf-okf-hygiene` corpus backfill — 8 legacy bundles | #316 |
| 3 | Regenerate user-facing docs; the `DRIFT-CHECK.md` manifest-coverage work | **#317** |

This issue proposes a new `yf-drift-check` edge, which makes it manifest-design work — #317 owns
`DRIFT-CHECK.md` §1/§2/§6 and every new edge. Plan 1 touched the manifest only where a stale
authority forced it (§3's `e-frontmatter` and §5's Install rows, which named an `install.py`
deleted at plan-010).

**One caveat for whoever picks this up.** plan-061's investigation (`findings/exp-004`) found
this issue's body to be **partly wrong** about the current state of the escape/stop taxonomy.
Re-derive before designing the edge rather than taking the body at face value.
