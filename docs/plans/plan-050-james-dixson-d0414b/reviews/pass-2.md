---
type: Review
okf_spec: OKF-PLAN
id: pass-2
status: complete
---

# Red-team pass 2

## Verdict: APPROVE

Second adversarial pass over the restructured plan. Every premise below was re-derived by running
a command, not by re-reading pass 1.

## Strengths

- **C1's restructure holds under the test that matters.** The gate's `Blocks` set (1.4, 2.4, 3.4,
  4.3, 5.2) and its evidence producers (1.1, 2.1, 3.1, 4.1) are disjoint, and every producer is an
  ancestor of what it unblocks. Verified against `plan_extract`'s edge list, not by reading prose.
- **Each fixture is a recorded historical failure, not a synthetic one** — plan-048's actual
  omitted-`#172` grant (3.1), the byte-identical PASS EXP-003 measured (2.1), the unstamped bead
  EXP-004 sampled (4.1). A fixture drawn from a real failure cannot be tuned to pass.
- The plan **declines a deliverable on evidence** (#177, D-6) rather than retaining it because it
  was already scoped. Across plans 047-049 the failure ran the other way every time.

## Concerns

| # | Severity | Concern | Recommendation |
| :-- | :-- | :-- | :-- |
| C6 | **high** | **SC7 would have failed for a benign reason.** It asserted corpus `files_checked` is unchanged across the verdict fix — but this plan writes `assets/red-prework.md` and `assets/redcheck.sh` into its own bundle under `docs/plans/`, which the corpus measurement selects. The criterion would have gone red because the plan added files, not because selection was perturbed | Measure SC7 with `--exclude` over this plan's own bundle — the #135 self-exclusion mechanism plan-049 shipped. **Applied** |
| C7 | medium | The driven-red gate's `Test` invokes `assets/redcheck.sh`, which does not exist until Issue 0.2 closes. At the execute-start sweep it will exit non-zero for "file not found" | Correct as written: the gate's own Instructions map a harness failure to **2 = UNRESOLVED**, which neither opens nor reds it. Recorded so a future reader does not "fix" it into a false red |
| C8 | medium | `SC12` and `SC2` both draw on `assets/red-prework.md`, so a single corrupted file could discharge two criteria | Accepted. The file is append-only per observation and each entry carries its command and exit code, so a partial write is detectable. Recorded rather than restructured |
| C9 | low | Epic 6 has six issues and no fixture of its own — it is pure landing | Correct by construction; landing epics are bookkeeping. Not marked `epic-kind: bookkeeping` because SC15/SC16/SC19/SC20 do discharge against it |

## Missing

None. `plan_extract`: 7 epics, 25 issues, 32 edges, 4 gates, 20 criteria, **0 unparsed**.
`doc_lint`: PASS, 0 findings at `review` severity. `audit`: pass.

## Gate Assessment

Unchanged from pass 1 and now verified mechanically: the driven-red gate's `Blocks` and evidence
sets are disjoint. The Upstream-write gate applies this plan's own #178 fix to itself — the grant
must be generated by the Issue 3.2 verb, which is the deliverable being gated. That is deliberate
dogfooding, and it is the only gate whose Instructions depend on an issue inside the same plan;
if 3.2 slips, the gate falls back to hand derivation and must say so.

## Upstream Assessment

Unchanged: 5 `include`, 4 `partial`, 1 `deferred`, 1 `exclude`, all `include` rows naming a
resolving issue. Re-verified after the renumbering — #179→1.2, #180→1.3, #181→2.2, #178→3.2,
#149→4.2, #182→5.1, #177→6.2.

## Resolutions

| Concern | Severity | Resolution | Actor | Status |
| :-- | :-- | :-- | :-- | :-- |
| C6 | high | SC7 now measures with `--exclude` over this plan's own bundle | `main-session` | resolved |
| C7 | medium | Confirmed correct as written; rationale recorded here | `main-session` | resolved |
| C8 | medium | Accepted with the shared-artifact hazard recorded | `main-session` | resolved |
| C9 | low | Confirmed correct; no change | `main-session` | resolved |
