---
type: Review
okf_spec: OKF-PLAN
id: pass-1
status: complete
---

# Red-team pass 1

## Verdict: REVISE

Performed in-session by the main session rather than by the `red-team` sub-agent: this session
runs under a standing constraint not to dispatch agents unasked. Every concern below is backed by
a command that ran, per the same evidence standard the agent is held to.

## Strengths

- Two experiments **refuted or revised their own scope decision before drafting** (EXP-001 removed
  a deliverable, EXP-004 replaced M9's premise). That is the failure mode plan-048 and plan-049
  each hit at execution instead.
- Every figure in the plan was measured in this session; nothing is inherited from research 004 as
  a local prevalence (D-5).
- EXP-006 produced an *honest narrowing*: the plan declines to claim #182 is mechanically
  enforceable (D-8) rather than implying prose has an exit code.

## Concerns

| # | Severity | Concern | Recommendation |
| :-- | :-- | :-- | :-- |
| C1 | **high** | **The Epic-0 gate blocked the issues that produce its own evidence.** Issue 0.3 required every control observed RED before its fix landed, but the controls were authored in 1.3/2.3/3.3 — *after* their fixes. A control cannot be observed failing before it exists. This is the "gate blocks the issue that produces its own evidence" class, inside the plan whose thesis is shown-red-before-trusted-green | Restructure every epic to control-before-fix: an `x.1` fixture issue records the RED observation against unfixed code, the fix depends on it, and a later issue records GREEN. Gate `Blocks` the GREEN verifications only, leaving the evidence producers outside |
| C2 | medium | Epic 4 mixed two unrelated axes — M9 attribution and the red-team rule rewrite — in one epic, so an epic-boundary report would conflate them | Split: Epic 4 is M9, Epic 5 is the rule, Epic 6 is landing |
| C3 | medium | `SC2` asserted the same property as the capability gate's Condition, so a single defect could be recorded twice as two independent confirmations | Kept deliberately: SC2 is discharged by the fixture issues, the gate by the harness. Different evidence, same claim — recorded here so a later reader does not read it as corroboration |
| C4 | low | The audit initially failed on `references/upstream-153.md: ../ parent traversal`, which is prose inside an issue body (`cargo:rerun-if-changed=../skills`), not a link | Escaped the prose. Worth noting the checker treats prose `../` as a traversal — a false-positive class adjacent to this plan's subject, but out of scope |
| C5 | low | `index.md`'s blurb still described the M11 spine the operator descoped at scoping | Rewritten to match the actual deliverable |

## Missing

None. All skeleton sections present; `plan_extract` reports `unparsed: 0`.

## Gate Assessment

Four gates. The Start Gate and Reconcile Gate are standard. The **driven-red gate** is the plan's
own instance of research 004's M11 pattern and, after C1, its evidence producers (1.1, 2.1, 3.1,
4.1) sit outside its `Blocks` set — the shape plan-049's SC35 established. The **Upstream-write**
gate is `human` and routes to the operator; its Instructions require the grant be generated by the
verb this plan ships, which is the #178 fix applied to itself.

## Upstream Assessment

12 rows triaged. Five `include` (#178-#182), four `partial`, one `deferred`, one `exclude`, and
#177 moved `include → partial` on EXP-001's refutation. Every `include` row names a resolving
issue. #177's `partial` requires a comment carrying the refutation so the next attempt does not
rebuild the same scanner — discharged by SC16.

## Resolutions

| Concern | Severity | Resolution | Actor | Status |
| :-- | :-- | :-- | :-- | :-- |
| C1 | high | Epics restructured control-before-fix; gate `Blocks` moved to 1.4, 2.4, 3.4, 4.3, 5.2; evidence producers 1.1/2.1/3.1/4.1 are ancestors outside the Blocks set | `main-session` | resolved |
| C2 | medium | Epic 4 split into Epic 4 (M9) and Epic 5 (the rule); landing moved to Epic 6 | `main-session` | resolved |
| C3 | medium | Retained with the double-counting hazard recorded in this file | `main-session` | resolved |
| C4 | low | Prose escaped in `references/upstream-153.md`; audit re-run and now `pass` | `main-session` | resolved |
| C5 | low | `index.md` blurb rewritten to the actual deliverable | `main-session` | resolved |
