---
type: Reference
okf_spec: OKF-PLAN
id: comment-150
description: Drafted upstream comment for #150 (partial — stays OPEN)
---

# Draft comment → #150 (`partial`; **stays OPEN**)

> **`plan-050-james-dixson-d0414b` delivered six of this research's ranked classes as worked
> instances. The mining programme itself stays open.**
>
> ## In
>
> Six mechanical process defects, each shipping with a control that was **observed RED before
> its fix landed**: `#178`, `#179`, `#180`, `#181`, `#186`, `#187`. All six are closed by that
> plan.
>
> These are instances of this research's headline — *a written rule that nothing executes is
> unreliably obeyed, and no exit code records the skip* — chosen because each had a
> **reproduction on `main`** rather than a hypothesis, and each had been hit by more than one
> plan.
>
> ## Out, and where each went
>
> - **M9** (remediation-edge attribution) → **plan-051**, with `#149`. Its premise was revised
>   by measurement first: the 26 `discovered-from` edges are intact and resolvable; only
>   *attribution* is missing, so it is a stamping gap rather than a missing relationship.
> - **The M11 probe mechanism** → unscheduled. Deliberately: it is better-evidenced *as a
>   general prescription* than any local instance, and building it against no failing instance
>   is the shape `#177` warns about. The fixing plan nonetheless instantiated M11 **once**, as
>   its own capability gate: the probe placed before the work that depends on it.
> - **The remaining 14 ranked classes** → unscheduled.
>
> ## One finding worth folding back into this research
>
> The plan's execution produced **three defects caught by RUNNING a control, none of which
> thirteen review cycles and eleven independent red-team passes had found**:
>
> - the driven-red harness itself reported `RED observed` and exited **0** for a fixture that
>   did not exist — a silent green in the instrument built to grade silent greens;
> - `ctl-180`'s RED **refuted the plan's own diagnosis** of `#180`;
> - `ctl-178`'s mandatory contrast arm caught the new grant generator being wrong **twice**.
>
> That is a measurement about the review process, not a moral: reading found none of them, and
> running found all three. It is direct evidence for `#188` (the structure-tested /
> payload-untested blind spot) and `#190` (the 80%-coverage policy). The entries are `RE-005`,
> `RE-007` and `RE-009` in
> `docs/plans/plan-050-james-dixson-d0414b/plan-retrospective.md`.
