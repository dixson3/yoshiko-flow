UPSTREAM-WRITE GATE — AUTHORIZED BY THE OPERATOR
plan: plan-051-james-dixson-2f499f
date: 2026-08-23
grant presented: assets/upstream-grant-proposal.md (generated by plan_manager.py grant,
                 reconciled exactly against plan.md's Upstream Issues table before the ask)

AUTHORIZED SCOPE — 7 comments, 2 closes, nothing else:
  comment + CLOSE : #182, #184
  comment only    : #149, #150, #165, #173, #174   (all five `partial` rows)

CONDITION OF THE GRANT: plan-050's pass-5 C40 correction folded into the #149 comment,
cited PATH-QUALIFIED to that bundle. Re-verified at source before publication:
change_validation.py run_command sets `inconclusive` only under `if not tool_on_path(tok)`
(:781-783); the normal path is `"status": "pass" if proc.returncode == 0 else "fail"` (:797),
so an exit-2 row reports `fail`, not `inconclusive`.

OPERATOR CONSTRAINTS ON THE WRITE:
  - route through /yf-beads-upstream; no hand-run raw push; gh owns its own credentials
  - verify each write STRUCTURALLY (URL on create, clean exit on edit); exit 0 is not proof
  - halt before any destructive follow-on if a write is unverified
  - close NOTHING beyond #182 and #184
  - `closable` at land-the-plane is PROPOSE-ONLY and comes back to the operator
  - Issue 4.3 files the coarse tracker THROUGH /yf-beads-upstream so the epic carries an
    external_ref (SC12b) — not a bare `gh issue create`
