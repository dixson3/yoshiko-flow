---
type: Retrospective
okf_spec: OKF-PLAN
---
# Plan retrospective

Stops and deviations recorded during execution, newest last. Each `## RE-NNN` section is
one entry; `RE-NNN` ids are append-only and are never reused or renumbered.

`detected_by` records WHO found the entry and `evidence` records the command and output
substantiating any state claim in it, or the literal `unverified`. Both exist because an
entry's trust level is a property of who found it, and the recorder is usually the subject:
a retrospective built from an actor's own account would faithfully transcribe a false claim
rather than detect one. A state assertion with no evidence is a narration, not a finding.

## RE-001

| field | value |
| :-- | :-- |
| `kind` | deviation |
| `when` | 2026-08-23 |
| `stop_class` |  |
| `asked` | Issue 0.3 mandates recording the corpus Verification: census; EXP-003 reported '1 of 251 clauses executes' |
| `answered` | Re-measured under six explicit pathspecs: P1 (this plan's declared pathspec) returns 221, and the count of WHOLE-LINE-BACKTICKED-COMMAND clauses at main is 0, not 1. EXP-003's '1' is true under a different definition (the clause NAMES a test that is CV-registered and green). Recorded both definitions in assets/baseline-pre-fix.md rather than picking one. |
| `frontloadable` | partial |
| `detected_by` | mechanical-check |
| `evidence` | git grep -h '^Verification:' main -- '*.md' ':!docs/plans' ':!docs/research' | grep -cE '^Verification: `[^`]*`$' -> 0; P1 count -> 221; P5 (untracked incl) -> 256 |
| `escape_class` |  |
| `adjudication` |  |
| `origin` |  |
| `culpability` |  |
| `prevention` |  |
| `cost` |  |

## RE-002

| field | value |
| :-- | :-- |
| `kind` | deviation |
| `when` | 2026-08-23 |
| `stop_class` |  |
| `asked` | Issue 3.2's test was written with _THIS_TEST as a hardcoded literal and a _req_block parser that searched for the next REQ- line from offset 0 |
| `answered` | Two defects, both caught mechanically rather than by reading: (a) the vacuity guard test_the_spec_is_parseable_at_all FAILED on the first run — every REQ block parsed EMPTY because the regex matched the REQ's own id line; (b) SC9's rename arm showed a hardcoded _THIS_TEST keeps the meta-assertion green after a rename. Both fixed; _THIS_TEST now derives from Path(__file__).name. Recorded in assets/sc9-non-rottable.md. |
| `frontloadable` | no |
| `detected_by` | mechanical-check |
| `evidence` | first run: 4 failed, 2 passed; SC9 arm A after the derive fix: 3 failed, 3 passed on a renamed copy |
| `escape_class` |  |
| `adjudication` |  |
| `origin` |  |
| `culpability` |  |
| `prevention` |  |
| `cost` |  |

## RE-003

| field | value |
| :-- | :-- |
| `kind` | deviation |
| `when` | 2026-08-23 |
| `stop_class` |  |
| `asked` | SC4b was measured green at Issue 1.2a (7 paths, strict subset). Was it still true at completion? |
| `answered` | NO. Issue 3.2 (commit 1eb9bae) added scripts/test_review_agent_contract.py, whose line 145 quotes 'never writes files' inside an assertion message, taking the hit set to 8 and OUT of subset. A criterion discharged at 1.2a was invalidated by a later epic. PROCESS FINDING, larger than the fix: nothing in this plan re-checks plan.md CRITERIA at completion. Issue 4.1 mandates re-running the three FIXTURES against the merged tree for exactly this stated reason ('a later epic can silently undo an earlier one's green with nothing detecting it') — but that mandate covers fixtures only, and SC4b is a criterion with no fixture. It was caught by an OPERATOR re-measurement, not by any mechanism this plan ships. Also an instance of the self-reference class (the checker became a member of the set it measures; cf. plan-050 C119). Resolved by option (a): an enumerated NO-EDIT row 18 with a quote-to-forbid disposition, citing the ratified REQ-BUP-053/GR-BUP-005 precedent. Option (c), excluding scripts/ from the pathspec, was rejected as narrowing the criterion to make it pass. |
| `frontloadable` | partial |
| `detected_by` | operator |
| `evidence` | git grep -lniE 'never writes? (any )?files' -- 'skills/yf-plan/**' 'web/content/**' -> 8 paths (was 7 at 1.2a); new hit skills/yf-plan/scripts/test_review_agent_contract.py:145, introduced by 1eb9bae; after row 18: hit set 8, unenumerated 0, SC4b PASS |
| `escape_class` |  |
| `adjudication` |  |
| `origin` |  |
| `culpability` |  |
| `prevention` |  |
| `cost` |  |

## RE-004

| field | value |
| :-- | :-- |
| `kind` | stop |
| `when` | 2026-08-23 |
| `stop_class` | 2 |
| `asked` | Upstream-write capability gate (human/consent): authorize 7 upstream comments and 2 closes against the grant generated by plan_manager.py grant? |
| `answered` | PENDING — presented to the operator; not yet answered. |
| `frontloadable` | no |
| `detected_by` | mechanical-check |
| `evidence` | gate Test 'test -f docs/plans/plan-051-james-dixson-2f499f/assets/upstream-authorization.txt' exits 1; gate_type=human, test_class=consent — a green test can never substitute for consent |
| `escape_class` |  |
| `adjudication` |  |
| `origin` |  |
| `culpability` |  |
| `prevention` |  |
| `cost` |  |

