---
name: yf-broken
description: A deliberately malformed skill frontmatter block — `skill-group` and `depends-on-tool` are both absent.
---

# yf-broken

Violates `skill/required-frontmatter`, which is `E` because the real corpus passes it 19/19.
