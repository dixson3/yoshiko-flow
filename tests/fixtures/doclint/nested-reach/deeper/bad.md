# A malformed finding, one level deeper than a single-star glob reaches

## Question
Missing `## Method`, `## Findings` and `## Implications` — three error-severity findings that
a recursive glob sees and a single-level glob does not.
