**Upstream #999** — a deliberately malformed upstream reference.

No heading, so `upstream-reference/has-heading` fails. The `## Body` section and the
`- **Number:**` metadata bullet are also absent; both are report-only because the glob's
population is mixed (9 of 194 real files are hand-authored, not producer-generated).
